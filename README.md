## Running the code

To run `isbn.rb`, in a terminal with Ruby 2.x+:

```sh
$ cd isbn
$ ruby isbn.rb
```

This will prompt for a 12-digit ISBN13 input in a loop, until `CTRL+C` is pressed.

## Tests

To run tests:

```sh
$ cd isbn
$ rspec
```
